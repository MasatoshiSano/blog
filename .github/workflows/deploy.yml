name: Deploy Blog

on:
  push:
    branches:
      - main
  repository_dispatch:
    types: [blog-rebuild]

permissions:
  id-token: write
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ap-northeast-1

      - name: Sync content from S3
        env:
          CONTENT_BUCKET: ${{ secrets.CONTENT_BUCKET_NAME }}
          MEDIA_BUCKET: ${{ secrets.MEDIA_BUCKET_NAME }}
        run: |
          aws s3 sync "s3://${CONTENT_BUCKET}/posts/" content/posts/ --delete
          aws s3 sync "s3://${MEDIA_BUCKET}/" public/media/ --delete

      - name: Build
        run: npm run build

      - name: Deploy to S3
        env:
          S3_BUCKET: ${{ secrets.S3_BUCKET_NAME }}
        run: aws s3 sync out/ "s3://${S3_BUCKET}" --delete

      - name: Invalidate CloudFront cache
        env:
          CF_DISTRIBUTION_ID: ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }}
        run: |
          aws cloudfront create-invalidation \
            --distribution-id "${CF_DISTRIBUTION_ID}" \
            --paths "/*"
